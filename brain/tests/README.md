# brain/tests

Sanity tests for the connectome graph and simulator.

Planned (M2/M3):

- `test_graph.py` — connectome loads, shapes and metadata line up
- `test_lif.py` — simulator is stable (no runaway firing / flatlining)
